---
fqdn: mail.law-orga.de

# This is largely, but not fully, server-agnostic.  (culprit: /var/mail)
#  - One might want to use the ansible.posix.mount module instead.
#    - Problem: We also need the fstab at setup time.
fstab_file: ../conf/fstab

tls_acme_contacts:
  - 'mailto:work-laworga-acme@respiranto.de'    # TODO: Change this.

tls_acme_domain_names:
  - '{{ fqdn }}'


# (PostgreSQL) database settings.
#  - See `/doc/database.md`, `/ansible/hosts.yaml`.

database_server_address: '{{ network.private_db.ipv4.db.address }}'
database_server_port: '{{ network.private_db.ipv4.db.address }}'
database_user: lawandorga-mail-server
database_password: '{{ secret_database_password }}'
database_name: lawandorga-mail-server
...
